interface:
  display_name: "内容工厂回归测试"
  short_description: "用于为 ACF 变更选择和实现相关回归、mock 或 replay 验证。"
  default_prompt: "请使用 $auto-content-factory-testing 处理当前任务，沿用项目边界并验证实际结果。"
policy:
  allow_implicit_invocation: true
