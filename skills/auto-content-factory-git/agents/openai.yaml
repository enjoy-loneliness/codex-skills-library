interface:
  display_name: 内容工厂 Git 交付
  short_description: 用于 ACF 改动的提交、PR 和交付说明，仅纳入已验证的任务文件。
  default_prompt: Use $auto-content-factory-git to prepare a conventional commit or
    PR summary.
policy:
  allow_implicit_invocation: true
