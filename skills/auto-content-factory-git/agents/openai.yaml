interface:
  display_name: "内容工厂 Git 交付"
  short_description: "用于 ACF 改动的提交、PR 和交付说明，仅纳入已验证的任务文件。"
  default_prompt: "请使用 $auto-content-factory-git 处理当前任务，沿用项目边界并验证实际结果。"
policy:
  allow_implicit_invocation: true
