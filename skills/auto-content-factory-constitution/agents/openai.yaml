interface:
  display_name: 内容工厂工作原则
  short_description: 用于判断 ACF 产品边界和跨模块决策；提供默认原则，不覆盖当前用户要求或现行 ADR。
  default_prompt: Use $auto-content-factory-constitution as the highest-priority rules
    for Auto Content Factory work.
policy:
  allow_implicit_invocation: true
