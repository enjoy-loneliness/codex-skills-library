interface:
  display_name: 内容工厂发布交接
  short_description: 用于 ACF 稿件的平台适配和已授权发布交接，保留实际发布边界。
  default_prompt: Use $auto-content-factory-publish to plan platform-specific publishing
    for a content draft.
policy:
  allow_implicit_invocation: true
