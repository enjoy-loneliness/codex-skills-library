interface:
  display_name: "内容工厂交付审查"
  short_description: "评审 ACF 改动的业务正确性与交付证据，只检查受影响流程和契约。"
  default_prompt: "请使用 $auto-content-factory-review 处理当前任务，沿用项目边界并验证实际结果。"
policy:
  allow_implicit_invocation: true
